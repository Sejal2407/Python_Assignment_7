# Python_Assignment_7

Uses psycopg2 to interface with the database.

CREATE TABLE IF NOT EXISTS prevents duplication of table error.

Student insert parameterized queries (%s) to dynamically insert data.

Uses fetchone() in a loop to retrieve data row-by-row.